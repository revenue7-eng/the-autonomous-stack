# Authentik secret key — generate with: openssl rand -hex 32
AUTHENTIK_SECRET_KEY=change-me-generate-a-random-key

# Authentik database password — generate with: openssl rand -hex 16
AUTHENTIK_DB_PASSWORD=change-me-generate-a-random-password
